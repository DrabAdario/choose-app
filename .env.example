# Optional: Supabase (https://supabase.com) — Postgres, auth, and Realtime in one place.
# Leave unset for local-only UI until you wire sync.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Local dev: omit or set to "/". CI sets this to "/<repo-name>/" for GitHub Pages.
# VITE_BASE_PATH=/
